using System;
namespace TimeIsHoney.TIH.Engine.Windows
{
    public class MenuWindowArea
    {
        /// <summary>
        /// Inside this class we'll define where to show the menu window
        /// and how big it should be.
        ///
        /// We can also have different screens e.g launch screen, menu screen,
        /// submenu screens and the main game screen. We can later decide if we
        /// want to move the Menu to screens. There is no screens directory yet.
        /// If we decided to do it later we'll need a structure for that.
        ///
        /// </summary>
        public MenuWindowArea()
        {
        }
    }
}
